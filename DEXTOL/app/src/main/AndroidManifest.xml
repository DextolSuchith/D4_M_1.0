<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dextol.dextol">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".Doctor_profile" />
        <activity android:name=".Diagnostics_profile" />
        <activity android:name=".Treatment_profile" />
        <activity android:name=".Medical_Store_profile" />
        <activity
            android:name=".Registration"
            />
        <activity
            android:name=".MainActivity"
            android:label="Dextol"
             />
        <activity android:name=".Add_Family" />
        <activity android:name=".Dashboard" />
        <activity android:name=".Appointments" />
        <activity android:name=".Consultation" />
        <activity android:name=".Orders" />
        <activity android:name=".Favourites" />
        <activity android:name=".Medical_Reports" />
        <activity android:name=".Settings" />
        <activity android:name=".Family_Form_holder" />
        <activity android:name=".Doctor_Booking" />
        <activity android:name=".Order_medicines" />
    </application>

</manifest>